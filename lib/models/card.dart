// Temporary just ui designing
class PaymentCard {
  int id;
  String title;
  String imgUrl;
  String cardNumber;
  bool isFavourite;

  PaymentCard(
      {this.id, this.title, this.imgUrl, this.cardNumber, this.isFavourite});
}
